# Порядок Реализации Компонентов

## Философия подхода

> "Make it work, make it right, make it fast" — Kent Beck

Мы идём **снизу-вверх**: сначала инфраструктура, потом сервисы, потом UI.  
Каждый этап заканчивается **работающим прототипом**, который можно протестировать.

---

## Фазы Реализации

### 🏗️ Фаза 0: Инфраструктура

**Цель**: Поднять окружение, чтобы сервисы могли работать.

| #   | Компонент       | Описание                                                    | Статус    |
| --- | --------------- | ----------------------------------------------------------- | --------- |
| 0.1 | Docker Compose  | PostgreSQL + NATS + LLM endpoint                            | ✅ Готово |
| 0.2 | Database Schema | Миграции для `jobs`, `scraping_targets`, `job_applications` | ⬜        |
| 0.3 | Shared Library  | Go модуль с общими типами, логированием, NATS клиентом      | ⬜        |

**Deliverable**: `docker compose up` поднимает всё, миграции применяются автоматически.

---

### 📥 Фаза 1: Collector — Минимальный

**Цель**: Научиться забирать вакансии из одного источника.

| #   | Компонент              | Описание                                                    | Приоритет |
| --- | ---------------------- | ----------------------------------------------------------- | --------- |
| 1.1 | TG Auth Tool           | Утилита для генерации session string (минимальный CLI)      | P0        |
| 1.2 | TG Channel Parser      | Парсинг публичного канала по username                       | P0        |
| 1.3 | TG Forum Topics        | Парсинг топиков (подчатов) внутри supergroup/канала         | P0        |
| 1.4 | Parsing Depth Config   | Условия остановки: `limit` ИЛИ `until` (первое выполненное) | P0        |
| 1.5 | Deduplication          | Проверка external_id перед вставкой                         | P0        |
| 1.6 | NATS Integration       | Публикация события `jobs.new`                               | P0        |
| 1.7 | REST API для запуска   | HTTP endpoint для триггера парсинга                         | P0        |
| 1.8 | REST API для остановки | HTTP endpoint для отмены текущего парсинга                  | P0        |
| 1.9 | File Logging           | Логирование в `.log` файл (вмонтирован в проект)            | P0        |

**Почему сначала Telegram?**

- Telegram каналы — самый стабильный источник вакансий
- Не требует браузерной эмуляции (проще)
- Меньше вероятность бана (в отличие от HH, LinkedIn)

**API Endpoints**:

```
POST /api/v1/scrape/telegram
{
  "channel": "@go_jobs",
  "limit": 100,                    // количество постов
  "until": "2025-01-01",           // дата до которой парсить
  "include_topics": true           // парсить ли все топики (подчаты)
}

// Парсинг останавливается если:
// - достигнут limit постов, ИЛИ
// - достигнута дата until (пост старше указанной даты)

DELETE /api/v1/scrape/current
// Останавливает текущий процесс парсинга
```

**Работа с Forum Topics (Подчаты)**:

Telegram supergroups могут быть сконфигурированы как форумы с топиками. Каждый топик — это отдельный подчат внутри группы.

**Как это работает:**

1. Канал/группа имеет флаг `forum=true`
2. У каждого топика есть уникальный `topic_id` (он же `message_thread_id`)
3. Топик "General" всегда имеет `id=1`
4. Получаем список топиков через `channels.getForumTopics`
5. Сообщения в топике имеют поле `reply_to.forum_topic=true`

```go
// Получение списка топиков в форуме
func getForumTopics(ctx context.Context, client *gotgproto.Client, channelID int64, accessHash int64) ([]ForumTopic, error) {
    inputChannel := &tg.InputChannel{
        ChannelID:  channelID,
        AccessHash: accessHash,
    }

    topics, err := client.API().ChannelsGetForumTopics(ctx, &tg.ChannelsGetForumTopicsRequest{
        Channel: inputChannel,
        Limit:   100,
    })
    if err != nil {
        return nil, fmt.Errorf("get forum topics: %w", err)
    }

    var result []ForumTopic
    switch t := topics.(type) {
    case *tg.MessagesForumTopics:
        for _, topic := range t.Topics {
            if ft, ok := topic.(*tg.ForumTopic); ok {
                result = append(result, ForumTopic{
                    ID:    ft.ID,
                    Title: ft.Title,
                })
            }
        }
    }
    return result, nil
}

// Получение сообщений из конкретного топика
func getTopicMessages(ctx context.Context, client *gotgproto.Client, peer tg.InputPeerClass, topicID int) ([]Message, error) {
    // используем GetReplies с указанием msg_id = topic_id
    replies, err := client.API().MessagesGetReplies(ctx, &tg.MessagesGetRepliesRequest{
        Peer:  peer,
        MsgID: topicID,
        Limit: 100,
    })
    if err != nil {
        return nil, fmt.Errorf("get topic messages: %w", err)
    }
    // parse messages...
    return messages, nil
}
```

**Логирование**:

- Логи пишутся в `./logs/collector.log`
- Формат: JSON lines для парсинга
- Rotation: ежедневно или по размеру (10MB)

**Deliverable**: API-сервис принимает запрос на парсинг TG канала (включая топики) и пишет результат в БД.

---

### 🧠 Фаза 2: Analyzer — Локальный AI

**Цель**: Автоматически извлекать структурированные данные из вакансий.

| #   | Компонент          | Описание                                          | Приоритет |
| --- | ------------------ | ------------------------------------------------- | --------- |
| 2.1 | LLM Client         | Универсальный HTTP клиент (совместим с LM Studio) | P0        |
| 2.2 | Extraction Prompt  | Промпт для извлечения данных с **JSON output**    | P0        |
| 2.3 | NATS Consumer      | Подписка на `jobs.new`, обработка очереди         | P0        |
| 2.4 | Batch Processing   | Асинхронная обработка `RAW` вакансий              | P0        |
| 2.5 | Language Detection | Определение языка (RU/EN) — включено в промпт     | P1        |

**LLM Integration**:

- Используем OpenAI-совместимый API (работает с LM Studio, Ollama, vLLM)
- URL задаётся через env: `LLM_BASE_URL=http://localhost:1234/v1`

**Extraction Prompt (JSON Output)**:

```
Извлеки структурированные данные из вакансии. Ответь ТОЛЬКО валидным JSON:

{
  "title": string | null,
  "description": string | null,
  "salary_min": number | null,
  "salary_max": number | null,
  "currency": "RUB" | "USD" | "EUR" | null,
  "location": string | null,
  "is_remote": boolean,
  "language": "RU" | "EN",
  "technologies": string[],
  "experience_years": number | null,
  "company": string | null,
  "contacts": string[]
}
```

**Поля**:

- `title` — название позиции
- `description` — краткое описание вакансии (1-2 предложения)
- `contacts` — массив контактов (может быть несколько: telegram, email, телефон)

**NATS Подробнее**:

NATS используется как message broker для event-driven архитектуры:

| Subject         | Publisher  | Subscriber      | Payload                     |
| --------------- | ---------- | --------------- | --------------------------- |
| `jobs.new`      | Collector  | Analyzer        | `{job_id, raw_content}`     |
| `jobs.analyzed` | Analyzer   | Web (WebSocket) | `{job_id, structured_data}` |
| `jobs.tailored` | Brain      | Dispatcher      | `{job_id, resume_path}`     |
| `jobs.sent`     | Dispatcher | Web (WebSocket) | `{job_id, delivery_status}` |
| `command.*`     | Web UI     | All services    | Command payloads            |

**Почему NATS?**

- JetStream для персистентности (сообщения не теряются)
- Простая модель pub/sub
- Минимальные накладные расходы
- Идеально для Go

**Deliverable**: Вакансии со статусом `RAW` автоматически получают `structured_data` (JSON) и переходят в `ANALYZED`.

---

### 🌐 Фаза 3: Web UI — MVP

**Цель**: Интерфейс для просмотра вакансий и управления.

| #   | Компонент              | Описание                                      | Приоритет |
| --- | ---------------------- | --------------------------------------------- | --------- |
| 3.1 | API Server             | Go HTTP API (Chi/Echo)                        | P0        |
| 3.2 | Jobs List              | Таблица вакансий с фильтрами                  | P0        |
| 3.3 | Job Detail             | Просмотр полной вакансии                      | P0        |
| 3.4 | Status Actions         | Кнопки: Interesting / Reject                  | P0        |
| 3.5 | Targets Config         | CRUD для `scraping_targets`                   | P1        |
| 3.6 | WebSocket Events       | Лента событий в реальном времени              | P2        |
| 3.7 | Pico.css Refactor      | Унификация стилей: semantic HTML, grid, forms  | P1        |

**Стек**:

- Backend: Go + Chi router
- Frontend: **HTMX + Pico.css**
- Theme: **Dark mode only**

**Deliverable**: Веб-интерфейс на `localhost:3100` показывает вакансии.

---

### 📝 Фаза 4: Brain + WebSocket Events

**Цель**: Генерация персонализированных резюме и детальная система событий для UI.

| #   | Компонент           | Описание                            | Приоритет |
| --- | ------------------- | ----------------------------------- | --------- |
| 4.1 | Base Resume Storage | Хранение базового резюме в Markdown | P0        |
| 4.2 | LLM Integration     | OpenAI-compatible API + NATS Queue  | P0        |
| 4.3 | Resume Tailoring    | Адаптация резюме под вакансию       | P0        |
| 4.4 | Cover Letter Gen    | Генерация сопроводительного письма  | P0        |
| 4.5 | PDF Renderer        | HTML → PDF (chromedp/headless)      | P0        |
| 4.6 | WebSocket Events    | Unified System for UI Updates       | P0        |
| 4.7 | API Documentation   | Scalar UI + OpenAPI Spec            | P1        |

**Технологии**:

- **PDF**: `chromedp` (headless chrome) + Go HTML templates
- **LLM**: Любой OpenAI-compatible API (через .env)
- **Docs**: Scalar (modern UI)
- **Queue**: NATS для асинхронной генерации

**Deliverable**:

1. Кнопка "Prepare Response" генерирует резюме и cover letter.
2. Весь UI обновляется в real-time через WebSocket (scraping, analysis, generation).

---

### 📤 Фаза 5: Dispatcher

**Цель**: Автоматическая отправка откликов.

| #   | Компонент         | Описание                                  | Приоритет |
| --- | ----------------- | ----------------------------------------- | --------- |
| 5.1 | TG DM Sender      | Отправка резюме и cover letter в Telegram | P0        |
| 5.2 | Email Sender      | Отправка резюме и cover letter через SMTP | P0        |
| 5.3 | Delivery Tracking | Логирование статуса доставки              | P0        |

**Deliverable**: Кнопка "Send to Telegram" / "Send Email" отправляет резюме и сопроводительное письмо рекрутеру.

---

### 📧 Фаза 6: Email Integration

**Цель**: Интеграция почты для получения кодов подтверждения (HH, LinkedIn).

| #   | Компонент      | Описание                             | Приоритет |
| --- | -------------- | ------------------------------------ | --------- |
| 6.1 | IMAP Client    | Подключение к почтовому ящику        | P0        |
| 6.2 | Code Extractor | Парсинг кодов подтверждения из писем | P0        |
| 6.3 | UI Integration | Показ кода в интерфейсе для ввода    | P0        |

**Зачем?**

- HH и другие сайты требуют email verification
- Автоматическое извлечение кодов ускоряет процесс

**Deliverable**: Система может читать почту и показывать коды подтверждения в UI.

---

### 🚀 Фаза 7: HH.ru Parser

**Цель**: Добавить дополнительный источник — HeadHunter.

| #   | Компонент        | Описание                             | Приоритет |
| --- | ---------------- | ------------------------------------ | --------- |
| 7.1 | Browser Strategy | Rod интеграция для headless browsing | P0        |
| 7.2 | HH Search Parser | Парсинг результатов поиска           | P0        |
| 7.3 | HH Detail Parser | Парсинг страницы вакансии            | P0        |
| 7.4 | Anti-Detection   | User-Agent, задержки, cookies        | P1        |

**Почему в конце?**

- Сложнее (требует браузер)
- Выше риск бана
- Требует интеграцию с Email для кодов подтверждения
- На Telegram уже будет отлаженная архитектура

**Deliverable**: API endpoint парсит HH и пишет в БД.

---

## Визуализация Прогресса

```
Фаза 0   Фаза 1      Фаза 2      Фаза 3    Фаза 4   Фаза 5       Фаза 6   Фаза 7
Infra    Collector   Analyzer    Web UI    Brain    Dispatcher   Email    HH Parser
  │         │           │           │         │          │          │         │
  ▼         ▼           ▼           ▼         ▼          ▼          ▼         ▼
[███]──→[█████]───→[████]────→[█████]──→[█████]──→[███]────→[███]───→[████]
  │         │           │           │         │          │          │         │
  └─────────┴───────────┴───────────┴─────────┴──────────┴──────────┴─────────┘
```

---

## Принципы

1. **Работающий код > Идеальный код**  
   Сначала запустим, потом рефакторим.

2. **Один источник > Много источников**  
   Telegram работает идеально → добавляем остальное.

3. **API-first подход**  
   Все действия через REST API, UI использует те же endpoints.

4. **Локальная LLM для фильтрации, Zai для генерации**  
   Экономим на рутине, используем мощную модель для важного.

5. **Docker Compose везде**  
   Никаких "у меня на машине работает".

---

## Решённые вопросы

| Вопрос            | Решение                                      |
| ----------------- | -------------------------------------------- |
| Frontend Stack    | Go + HTMX + Pico.css                         |
| Theme             | Dark mode only                               |
| Port              | localhost:3100                               |
| PDF Template      | Собственный HTML+CSS шаблон                  |
| LLM for tailoring | Zai                                          |
| LinkedIn          | Later (после стабилизации остального)        |
| Логирование       | В файл `./logs/*.log`, добавлен в .gitignore |

## Открытые вопросы

1. **Scheduling**:

   - Cron внутри контейнера
   - Kubernetes CronJob (если планируем K8s)
   - Отдельный Scheduler сервис

2. **Metrics/Monitoring**:
   - Prometheus + Grafana (production-ready)
   - Простые логи (MVP)
   - Когда добавлять?
