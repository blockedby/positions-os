# integration_test.go

Integration tests for LLM client — requires real LLM endpoint.

**Prerequisites:**
- `LLM_BASE_URL` pointing to running LLM server
- Valid `LLM_API_KEY` if required

**Test validates:** End-to-end extraction with real LLM response
