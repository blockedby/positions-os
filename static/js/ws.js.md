# ws.js

WebSocket connection handler for real-time updates.

Features:
- Auto-reconnect on connection close
- JSON message parsing and event dispatching
- Integration with HTMX for triggering updates
- Error handling and connection state logging
