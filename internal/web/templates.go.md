# templates.go

Go template engine for HTML rendering.

- `LoadTemplates()` — Parses templates from directory
- `Render()` — Executes template with data
- Default extension: `.html`
- Used for server-side rendered pages
