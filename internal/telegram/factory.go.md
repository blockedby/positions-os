# factory.go

Client factory and initialization.

- `NewClient()` — Creates gotgproto client from session string
- `NewClientFromAuth()` — Interactive phone auth flow
- Configures flood wait handlers
- Sets up error logging
